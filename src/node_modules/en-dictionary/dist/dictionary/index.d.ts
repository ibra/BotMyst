import Database from '../database';
import Data from '../parser/interface.data';
import Index from '../parser/interace.index';
import SearchSimple from './interface.searchSimple';
declare class Dictionary {
    path: string;
    database: Database;
    constructor(path: string);
    init(): Promise<void>;
    searchFor(term: string[]): Map<string, Index>;
    searchOffsetsInDataFor(offsets: number[]): Map<number, Data>;
    searchSimpleFor(words: string[]): Map<string, SearchSimple>;
    wordsStartingWith(prefix: string): string[];
    wordsEndingWith(suffix: string): string[];
    wordsIncluding(word: string): string[];
    wordsUsingAllCharactersFrom(query: string, ignorePhrases?: boolean): string[];
    wordsWithCharsIn(query: string, priorityCharacters?: string): Map<string, SearchSimple>;
    static hasAllCharsIn(word: string, test: string): boolean;
}
export default Dictionary;
